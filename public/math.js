// A few math functions and constants for convenience

var pi = Math.PI;

var abs = Math.abs;
var sqrt = Math.sqrt;
var pow = Math.pow;

var atan2 = Math.atan2;
var sin = Math.sin;
var cos = Math.cos;

var floor = Math.floor;

function sign(x) {
    return x ? (x < 0 ? -1 : 1) : 0;
}
